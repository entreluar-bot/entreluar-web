import Image from"next/image";import Link from"next/link";import type{JournalPost}from"../types";
export default function JournalCard({post,href}:{post:JournalPost;href:string}){const copy=post.content.replace(/<[^>]+>/g,"").slice(0,128);return <article className="luxe-card group flex h-full flex-col">{post.image_url&&<div className="relative aspect-[4/3] overflow-hidden"><Image src={post.image_url} alt={post.title} fill sizes="(max-width:760px) 100vw,33vw" className="object-cover transition duration-700 group-hover:scale-105" unoptimized/></div>}<div className="flex flex-1 flex-col p-6"><p className="eyebrow">{post.category||"Papo de Mulher"} • {new Date(post.created_at).toLocaleDateString("pt-BR")}</p><h2 className="font-display mt-3 text-3xl leading-none">{post.title}</h2><p className="muted my-4 flex-1 text-sm leading-7">{copy}{post.content.length>128?"…":""}</p><Link href={href} className="ghost-button">Continuar a conversa →</Link></div></article>}
