"use client"; import { useState } from "react"; import { createClient } from "@/utils/supabase/client"; export default function LoginPage() { const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [loading, setLoading] = useState(false); const [message, setMessage] = useState(""); const supabase = createClient(); const handleLogin = async (e: React.FormEvent) => { e.preventDefault(); setLoading(true); setMessage(""); const { error } = await supabase.auth.signInWithPassword({ email, password }); if (error) { setMessage("Erro ao entrar: " + error.message); } else { window.location.href = "/admin"; } setLoading(false); }; return ( <div className="min-h-screen bg-[var(--color-wine-dark)] flex items-center justify-center p-4"> <form onSubmit={handleLogin} className="bg-[var(--color-wine)] p-8 rounded-2xl shadow-xl w-full max-w-md border border-[var(--color-wine-light)]"> <div className="text-center mb-8"> <span className="text-4xl text-[var(--color-gold)] mb-2 block">☾</span> <h1 className="text-2xl font-serif text-[var(--color-gold)]">Acesso Secreto</h1> </div> {message && <p className="bg-red-900/50 text-red-200 p-3 rounded mb-4 text-sm">{message}</p>} <div className="space-y-4 mb-8"> <div> <label className="block text-[var(--color-gold-light)] text-sm mb-2">E-mail</label> <input type="email" value={email} onChange={(e) => setEmail(e.target.value)} required className="w-full bg-[var(--color-wine-dark)] border border-[var(--color-wine-light)] rounded px-4 py-3 text-[var(--color-gold-light)] focus:outline-none focus:border-[var(--color-gold)]" /> </div> <div> <label className="block text-[var(--color-gold-light)] text-sm mb-2">Senha</label> <input type="password" value={password} onChange={(e) => setPassword(e.target.value)} required className="w-full bg-[var(--color-wine-dark)] border border-[var(--color-wine-light)] rounded px-4 py-3 text-[var(--color-gold-light)] focus:outline-none focus:border-[var(--color-gold)]" /> </div> </div> <button type="submit" disabled={loading} className="w-full bg-gradient-to-r from-[var(--color-gold)] to-[#b5952f] text-[var(--color-wine-dark)] py-3 rounded font-bold uppercase tracking-widest hover:scale-[1.02] transition-transform"> {loading ? "Entrando..." : "Entrar"} </button> </form> </div> ); }
