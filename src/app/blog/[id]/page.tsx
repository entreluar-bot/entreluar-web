import Image from"next/image";import Link from"next/link";import{notFound}from"next/navigation";import{createClient}from"@/utils/supabase/server";import NewsletterSignup from"../../ui/NewsletterSignup";import type{JournalPost}from"../../types";
export const revalidate=0;export default async function BlogPost({params}:{params:Promise<{id:string}>}){const{id}=await params;const supabase=await createClient();const{data}=await supabase.from("journal").select("*").eq("id",id).single();if(!data)notFound();const p=data as JournalPost;return <main className="site-shell"><article className="article-shell luxe-card"><div className="p-5 md:p-8"><Link href="/blog" className="eyebrow inline-flex">← Voltar para as conversas</Link></div>{p.image_url&&<div className="relative aspect-[16/10]"><Image src={p.image_url} alt={p.title} fill priority sizes="(max-width:900px) 100vw,840px" className="object-cover" unoptimized/></div>}<header className="p-6 md:p-12"><p className="eyebrow">{p.category||"Papo de Mulher"} • {new Date(p.created_at).toLocaleDateString("pt-BR")}</p><h1 className="section-title my-6">{p.title}</h1><div className="prose-luxe" dangerouslySetInnerHTML={{__html:p.content}}/><section className="newsletter-cta newsletter-cta--article" aria-labelledby="post-newsletter-title"><div><p className="eyebrow">Entreluar por email</p><h2 id="post-newsletter-title" className="font-display text-4xl leading-none text-[var(--champagne-pale)]">A próxima conversa pode chegar primeiro para você.</h2><p className="muted mt-3 leading-7">Novidades 50+, autocuidado sem manual, convites especiais e papo de mulher para ler com calma.</p></div><NewsletterSignup source="blog-post"/></section></header></article></main>}
