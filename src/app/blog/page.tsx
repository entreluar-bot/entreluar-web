import { createClient } from "@/utils/supabase/server"; export default async function BlogPage() { const supabase = await createClient(); const { data: posts } = await supabase.from("journal").select("*").order("created_at", { ascending: false }); return ( <> <div className="stars"></div> <div className="min-h-screen flex flex-col items-center p-8 relative"> <nav className="w-full max-w-5xl flex justify-between items-center py-6 border-b border-[var(--color-wine-light)] mb-16"> <div className="flex items-center gap-2"> <a href="/" className="text-3xl text-[var(--color-gold)] hover:scale-110 transition-transform">☾</a> <a href="/" className="text-2xl font-serif text-[var(--color-gold)] tracking-widest uppercase">Entreluar</a> </div> <div className="hidden md:flex gap-8 text-[var(--color-gold-light)] text-sm uppercase tracking-widest"> <a href="/#vitrine" className="hover:text-[var(--color-gold)] transition-colors">Coleção Lunar</a> <a href="/blog" className="text-[var(--color-gold)] transition-colors">O Diário</a> <a href="/admin/login" className="hover:text-[var(--color-gold)] transition-colors">Acesso Secreto</a> </div> </nav> <main className="w-full max-w-4xl flex-1"> <h1 className="text-5xl md:text-6xl font-serif text-[var(--color-gold)] mb-4 text-center">O Diário</h1> <p className="text-xl text-[var(--color-gold-light)] mb-16 font-light text-center opacity-80"> Reflexões, descobertas e segredos da beleza madura. </p> {(!posts || posts.length === 0) ? ( <p className="text-center text-[var(--color-gold-light)] opacity-70">O diário está em branco. Em breve novas histórias.</p> ) : ( <div className="space-y-16"> {posts.map((post: any) => ( <article key={post.id} className="bg-[var(--color-wine)]/80 border border-[var(--color-wine-light)] rounded-3xl p-8 md:p-12 shadow-xl"> {post.image_url && ( <div className="w-full h-64 md:h-96 mb-8 overflow-hidden rounded-2xl border border-[var(--color-gold)]/30"> <img src={post.image_url} alt={post.title} className="w-full h-full object-cover" /> </div> )} <h2 className="text-3xl font-serif text-[var(--color-gold)] mb-6">{post.title}</h2> <div className="text-[var(--color-gold-light)] font-light leading-loose space-y-6 opacity-90 prose prose-invert prose-p:text-[var(--color-gold-light)] prose-strong:text-[var(--color-gold)] prose-h3:text-[var(--color-gold)] prose-h3:font-serif" dangerouslySetInnerHTML={{ __html: post.content }} /> </article> ))} </div> )} </main> <footer className="mt-24 pt-8 border-t border-[var(--color-wine-light)] w-full max-w-5xl flex justify-between items-center text-sm text-[var(--color-gold-light)] opacity-60"> <p>Entreluar Beauty © 2024</p> <p>O segredo das estrelas.</p> </footer> </div> </> ); }
