import { NextResponse } from "next/server"; import { GoogleGenAI } from "@google/genai"; const ai = new GoogleGenAI({ apiKey: process.env.GEMINI_API_KEY }); export async function POST(req: Request) { try { const { title, link, impressions } = await req.json(); const prompt = `Você é a Luana, o alter ego da marca Entreluar Beauty, uma mulher madura, elegante, acolhedora e sábia. Você está escrevendo uma recomendação de produto para a sua vitrine/blog. Detalhes do Produto: - Título/Nome: ${title} - Link do produto: ${link} - Minhas impressões pessoais (da fundadora): ${impressions} Tarefa: Escreva um pequeno parágrafo (máximo 4 linhas) como a Luana, recomendando este produto para o público de mulheres maduras. Use um tom de "conversa entre amigas", destacando autocuidado e valorizando o envelhecimento natural. Comece o texto de forma cativante. Não use hashtags.`; const response = await ai.models.generateContent({ model: "gemini-2.5-flash", contents: prompt, }); return NextResponse.json({ text: response.text }); } catch (error: any) { return NextResponse.json({ error: error.message }, { status: 500 }); } }
