import { NextResponse } from "next/server"; import { GoogleGenAI } from "@google/genai"; const ai = new GoogleGenAI({ apiKey: process.env.GEMINI_API_KEY }); export async function POST(req: Request) { try { const { title, link, impressions, imageUrl } = await req.json(); let imagePart = null; if (imageUrl && imageUrl.startsWith("http")) { const imgRes = await fetch(imageUrl); const arrayBuffer = await imgRes.arrayBuffer(); const buffer = Buffer.from(arrayBuffer); const mimeType = imgRes.headers.get("content-type") || "image/jpeg"; imagePart = { inlineData: { data: buffer.toString("base64"), mimeType } }; } const prompt = `Você é a Luana, o alter ego da marca Entreluar Beauty, uma mulher madura, elegante, acolhedora e sábia. Você está escrevendo uma recomendação de produto para a sua vitrine/blog. Detalhes fornecidos: - Título/Nome base: ${title} - Link de compra: ${link} - Minhas impressões pessoais (da fundadora): ${impressions} Tarefa Múltipla: 1. Analise a imagem fornecida (se houver) para identificar exatamente qual é o produto. 2. Use a ferramenta de Pesquisa do Google para buscar os ingredientes, funções principais e benefícios oficiais deste produto na internet. 3. Escreva um texto final de recomendação (em um único parágrafo fluido de até 5 linhas) combinando as suas descobertas técnicas (ingredientes/função) com as minhas impressões pessoais. 4. O tom deve ser de "conversa entre amigas", destacando autocuidado, valorizando o envelhecimento natural, e sempre em primeira pessoa (como Luana).`; const contents = []; if (imagePart) contents.push(imagePart); contents.push(prompt); const response = await ai.models.generateContent({ model: "gemini-2.5-flash", contents, config: { tools: [{ googleSearch: {} }] } }); return NextResponse.json({ text: response.text }); } catch (error: any) { return NextResponse.json({ error: error.message }, { status: 500 }); } }
