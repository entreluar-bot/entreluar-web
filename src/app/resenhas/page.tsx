import{createClient}from"@/utils/supabase/server";import JournalCard from"../ui/JournalCard";import type{JournalPost}from"../types";
export const revalidate=0;export default async function Resenhas(){const supabase=await createClient();const{data}=await supabase.from("journal").select("*").eq("category","Estudei para te explicar").order("created_at",{ascending:false});const posts=(data||[])as JournalPost[];return <main className="site-shell"><div className="content-wrap"><header className="page-intro"><p className="eyebrow">Ciência traduzida em conversa</p><h1 className="section-title mt-4">Estudei para<br/><em>te explicar</em></h1><p>Eu pesquiso os ativos, separo promessa de evidência e te conto o que realmente importa — sem jaleco imaginário e sem enrolação.</p></header>{posts.length?<div className="editorial-grid">{posts.map(p=><JournalCard key={p.id} post={p} href={`/resenhas/${p.id}`}/>)}</div>:<div className="empty-state">A lupa está trabalhando. Em breve tem ciência explicada como conversa de amiga. 🔬</div>}</div></main>}
